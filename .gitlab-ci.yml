services:
  - docker:dind
  
variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_DRIVER: overlay2

build:
  image: maven:3-jdk-8
  stage: build
  script:
    - mvn install
  artifacts:
    paths:
      - target/*.jar

publish on Gitlab:
  image: docker:19.03.0
  stage: deploy
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY  
    - docker build -t $CI_REGISTRY_IMAGE:latest .
    - docker push $CI_REGISTRY_IMAGE:latest

publish on Docker Hub:
  image: docker:19.03.0
  stage: deploy
  script:
    - echo "$DOCKER_REGISTRY_PASS" | docker login index.docker.io --username $DOCKER_REGISTRY_USER --password-stdin
    - docker build -t index.docker.io/$DOCKER_REGISTRY_USER/$CI_PROJECT_NAME:latest .
    - docker push index.docker.io/$DOCKER_REGISTRY_USER/$CI_PROJECT_NAME:latest
